name: Deploy ReVisit to GitHub Pages

on:
  push:
    branches: [ main, master, deploy ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install dependencies
        run: yarn install --frozen-lockfile
        
      - name: Build application
        run: yarn build
        env:
          VITE_BASE_PATH: "/MSZ-Project3/"
          
      - name: Create 404.html for SPA routing
        run: cp dist/index.html dist/404.html
        
      - name: List build output (debug)
        run: |
          echo "=== Dist directory contents ==="
          ls -la dist/
          echo "=== Basic questionnaire study contents ==="
          ls -la dist/basic-questionnaire-study/ || echo "basic-questionnaire-study directory not found"
          echo "=== Global.json exists? ==="
          ls -la dist/global.json || echo "global.json not found"
        
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4
        
      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4